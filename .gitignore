_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
.bundle/
.DS_Store

# Vale — style packages are regenerable via `vale sync`
.vale/styles/

# PDF rendering: canonical copy lives in Zenodo, regenerable via pandoc+Chrome headless
compliance-theater.pdf
